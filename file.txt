# resolve conflictHello from main branch updated
git add file.txtgit add file.txt git commit -m "Resolved merge conflict" git commit -m "Resolved merge conflict" git push origin main git branch -D feature git reflog 
git checkout -b feature commitID
